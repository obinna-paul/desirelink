-- Add the SEEKER account type: someone looking for a relationship, not to post
-- premium content or subscribe to creators. Behaves like EXPLORER everywhere in
-- the app except its accent color.
ALTER TYPE "ProfileType" ADD VALUE 'SEEKER';
